-- =========================================================
--  Міграція 02: деталі ціни для турів (тарифи по групах)
--  Запусти у Supabase -> SQL Editor -> Run. Безпечно повторювати.
-- =========================================================

-- Текстове поле під «1–6 — $480, 7–12 — $X, від 13 — індивідуально».
-- Картка показує число (price), сторінка туру — ці деталі.
alter table tours add column if not exists price_details text;

-- Валюта за замовчуванням — долар (для нових турів).
alter table tours alter column currency set default 'USD';
